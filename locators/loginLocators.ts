export default class loginLocators{

    static tfUsername = '#username'
    static tfPassword = '#password'
    static messageValidation = '.smaller-75'
    static messageErrorLogin2 = '.alert alert-danger > p'
    static messageErrorLogin = '.alert-danger > p'
    static btLogin = '.width-40'
}